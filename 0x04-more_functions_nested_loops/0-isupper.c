#include "main.h"
/**
 * _isupper - function that checks for uppercase charecter.
 * @c: input
 * Return: 1 if c is uppercase, 0 otherwise
 */
{
	if ((c >= 'A') && (c <= 'z'))
	
	return (1);

	else 
	return (0);
}
