#include "main.h"
/**
 * _isdigit - function that checks for a digit (0 through 9)
 * ok: input
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{

if (((>= 48) && (c <= 57))
return (0);
else
return (0);
}
